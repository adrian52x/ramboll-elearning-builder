// UI State Types
// These are derived types used for component state management

// import { Block, Step } from './api-responses';
// import { BlockType } from './enums';

// // Block without ID (for creating new blocks in UI)
// export type BlockDTO = Omit<Block, 'id'>;

// // Step-Block relationship for UI state
// export type StepBlockDTO = {
//     block: BlockDTO;
//     orderIndex: number;
// };

// // Step without ID (for creating new steps in UI)
// export type StepDTO = Omit<Step, 'id' | 'stepBlocks'> & {
//     stepBlocks: StepBlockDTO[];
// };
